node_modules
dist
dist-ssr
.output
.vinxi
.nitro
.tanstack
.wrangler
__unconfig*
*.local


# Data store (file-based DB, excluded from version control)
data/

# Dashboard build output (generated, served by server)
apps/server/public/

# OS
.DS_Store
*.pem

# Env files
.env
.env.*
!.env.example

# Root-level server config (production credentials, never commit)
/rollhook.config.yaml

# E2E generated config (machine-specific absolute paths)
e2e/rollhook.config.yaml

# E2E generated .env for docker-compose image tag switching
examples/bun-hello-world/.env

# Lighthouse exports
*.com-*.json

# Editor
.vscode/*
!.vscode/settings.json
!.vscode/extensions.json

# RALPH loop state (machine-specific, resumable but not committed)
.ralph-tasks.json
.ralph-logs/
